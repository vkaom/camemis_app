import * as types from '../actions/actionTypes';

const initialState = {SCHOOL_NAME:null, MOBILE_URL: null , MOBILE_CODE: null, LANGUAGE: null};
export function schoolSetting(state = initialState, action = {}) {
      return state;
}
